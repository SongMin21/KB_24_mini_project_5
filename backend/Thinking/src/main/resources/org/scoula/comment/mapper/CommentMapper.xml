<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.scoula.comment.mapper.CommentMapper">
    <update id="updateComment">
        UPDATE comment_tbl
        SET content = #{content}
        WHERE id = #{id}
    </update>
    <select id="selectComment" resultType="org.scoula.comment.domain.CommentVO">
        SELECT
            id,
            thinking_id,
            content,
            created_at,
            updated_at
        FROM comment_tbl
        WHERE thinking_id = #{thinking_id}
        ORDER BY created_at ASC
    </select>

</mapper>